*~
*.diff
*.orig
*.patch
*.rej
core
.gdb_history
.gdbinit
.DS_Store
# Meson build
/builddir
build/
build-*/
# IDE settings
.vscode/
.idea/
*.swp
*.swo
# Meson subproject machinery — wraplock files are generated alongside
# our git submodules and would conflict with the SHA tracking we get
# from git itself.
subprojects/.wraplock
subprojects/*.wrap.json
subprojects/packagecache/
