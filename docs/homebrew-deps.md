# Homebrew dependencies for building iputils on macOS

# Required for I18N (gettext support)

brew install gettext

# Other common dependencies (add as needed)

brew install meson ninja
brew install libidn2

# If you use additional libraries, add them here

# brew install <other-dependency>
